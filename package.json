{
  "name": "pi-hawk-provider",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi extension: Hawk OAuth provider with OpenAI and Anthropic routing",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^4.0.18"
  }
}
